const pathAuth = {
  login: '/login',
  register: '/register'
}

const pathUtil = {
  home: '/',
  notFound: '/404',
  none: ''
}

const pathMe = {
  profile: '/profile'
}

export { pathAuth, pathUtil, pathMe }
